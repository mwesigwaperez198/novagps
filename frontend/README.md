# NOVA Frontend

Terminal-first dashboard for NOVA GPS with a Three.js 3D mission view.

```bash
npm install
npm run dev
```

Environment:

```bash
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8765
```
